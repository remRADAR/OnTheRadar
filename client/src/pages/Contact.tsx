// Editorial Signal style: contact is a high-contrast split spread with a calm, form-first conversion path.
import { FormEvent, useState } from 'react';
import { ArrowUpRight, Check, Play } from 'lucide-react';
import { media, faqs, siteSettings } from '@/data/siteContent';

export default function Contact() { const [sent, setSent] = useState(false); const submit = (e: FormEvent) => { e.preventDefault(); setSent(true); }; return <div className="inner-page contact-page"><section className="contact-hero section-pad"><div className="contact-image"><img src={media.portfolio01.src} alt={media.portfolio01.alt} /><span className="image-stamp">ON / AIR</span></div><div className="contact-form-wrap"><div className="section-label"><span>(01)</span><span>Get in touch</span><span>© 2026</span></div><h1>Make the<br /><em>next move.</em></h1><p>Pick a plan, submit a job request, and your brief will kick off within 24 hours.</p>{sent ? <div className="form-success"><Check size={24} /><h2>Signal received.</h2><p>We’ll be in touch shortly at the email you shared.</p></div> : <form onSubmit={submit}><div className="form-row"><label>First name*<input required placeholder="Jim" name="firstName" /></label><label>Last name*<input required placeholder="Hopper" name="lastName" /></label></div><div className="form-row"><label>Category<select name="category" defaultValue=""><option value="" disabled>Select</option><option>Art Direction</option><option>Photography</option><option>Strategy</option></select></label><label>Email<input required type="email" placeholder="hello@mail.com" name="email" /></label></div><label>Message<textarea required placeholder="Tell us what you’re building..." name="message" rows={5} /></label><button className="submit-button" type="submit">Submit brief <ArrowUpRight size={18} /></button></form>}</div></section><section className="section-pad contact-faq"><div className="section-label"><span>(02)</span><span>Frequently asked questions</span><span>© 2026</span></div><div className="faq-layout"><div className="showreel-card"><img src={media.service.src} alt={media.service.alt} /><a href="https://www.youtube.com/" target="_blank" rel="noreferrer"><Play size={18} fill="currentColor" /> Play showreel</a></div><div className="faq-list">{faqs.map(item => <details key={item.question}><summary>{item.question}<span>+</span></summary><p>{item.answer}</p></details>)}</div></div></section><section className="contact-email section-pad"><p className="eyebrow">Or skip the form</p><a href={`mailto:${siteSettings.email}`}>{siteSettings.email} <ArrowUpRight size={22} /></a></section></div>; }
