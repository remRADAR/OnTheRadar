// Editorial Signal style: detail pages use one large image, short copy, and precise metadata instead of a generic case-study template.
import { ArrowLeft, ArrowUpRight } from 'lucide-react';
import { Link, useRoute } from 'wouter';
import { projects } from '@/data/siteContent';

export default function ProjectDetail() { const [, params] = useRoute('/work/portfolio/:slug'); const project = projects.find(item => item.slug === params?.slug); if (!project) return <div className="not-found section-pad"><p className="eyebrow">404 / Signal lost</p><h1>That project<br /><em>isn’t here.</em></h1><Link href="/work/portfolio" className="signal-button"><ArrowLeft size={17} /> Back to portfolio</Link></div>; return <div className="detail-page"><section className="detail-hero section-pad"><Link href="/work/portfolio" className="back-link"><ArrowLeft size={16} /> All work</Link><div className="section-label"><span>({project.index})</span><span>{project.category}</span><span>© {project.year}</span></div><h1>{project.title}</h1><p>{project.description}</p><div className="detail-image"><img src={project.image.src} alt={project.image.alt} /></div></section><section className="detail-foot section-pad"><p className="eyebrow">The brief / The signal</p><h2>Make a clear impression<br /><em>before the first word.</em></h2><Link href="/contact" className="signal-button">Start a project <ArrowUpRight size={17} /></Link></section></div>; }
